import type { JsonRpcSigner, TransactionRequest, Web3Provider } from '@ethersproject/providers';
import BigNumber from 'bignumber.js';
import type { Tx, TokenConfig } from '@irys/upload-core';
import { BaseWebToken } from '@irys/web-upload/tokens/base';
import { InjectedTypedEthereumSigner, type InjectedTypedEthereumSignerMinimalSigner } from '@irys/bundles/web';
export type MinimalSigner = InjectedTypedEthereumSignerMinimalSigner & Pick<JsonRpcSigner, 'sendTransaction' | 'estimateGas' | 'getGasPrice' | 'populateTransaction'>;
export type MinimalProvider = {
    getSigner: () => MinimalSigner;
} & Pick<Web3Provider, 'getTransaction' | 'getNetwork' | '_ready' | 'send' | 'estimateGas' | 'getGasPrice' | 'getTransactionCount'>;
export declare class EthereumConfig extends BaseWebToken {
    signer: InjectedTypedEthereumSigner;
    wallet: MinimalProvider;
    w3signer: MinimalSigner;
    providerInstance: MinimalProvider;
    readonly inheritsRPC = true;
    constructor(config: TokenConfig);
    getTx(txId: string): Promise<Tx>;
    ownerToAddress(owner: any): Promise<string>;
    sign(data: Uint8Array): Promise<Uint8Array>;
    getSigner(): InjectedTypedEthereumSigner;
    verify(pub: any, data: Uint8Array, signature: Uint8Array): Promise<boolean>;
    getCurrentHeight(): Promise<BigNumber>;
    getFee(amount: BigNumber.Value, to?: string): Promise<BigNumber>;
    sendTx(data: TransactionRequest): Promise<string | undefined>;
    createTx(amount: BigNumber.Value, to: string, _fee?: string): Promise<{
        txId: string | undefined;
        tx: any;
    }>;
    getPublicKey(): Promise<string | Buffer>;
    ready(): Promise<void>;
}
export default EthereumConfig;
